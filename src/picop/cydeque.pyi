"""Public :mod:`cypy.cydeque` stubs (signatures + docstrings for IDE / typecheckers)."""

def deque_eq(a: object, b: object) -> bool:
    """Return True if deques are equal (identity short-circuit + richcompare; same as ``deque.__eq__``)."""
    ...
