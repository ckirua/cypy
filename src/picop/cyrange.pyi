"""Public :mod:`cypy.cyrange` stubs (signatures + docstrings for IDE / typecheckers)."""

def range_eq(a: object, b: object) -> bool:
    """Return True if ranges represent the same sequence (identity + richcompare; same as ``range.__eq__``)."""
    ...
